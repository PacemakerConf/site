<% if @speaker.errors.any? %>
    <div id="error_explanation">
    	<div class="alert alert-dismissible alert-danger">
      		<button type="button" class="close" data-dismiss="alert">×</button>
      		<strong>Oh snap!</strong>
      <ul>
      <% @speaker.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
      </div>
    </div>
  <% end %>
  
<fieldset class='col-lg-8'>

<% if Invitation.where(email_hash: params[:hash], status: 'New').count > 0 %>
		<% @invite = Invitation.where(email_hash: params[:hash])[0] %>
		<% current_user = {'role' => User::SPEAKER}%>
    <% @readonly = false %>
    <legend>New Speaker</legend>
		<%= render 'form' %>
<% else %>

<%= render 'sorry'%>

<% end %>

</fieldset>
