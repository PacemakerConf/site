<fieldset class='col-lg-8'>

<legend>Event</legend>

<% Invitation.all.each do |invite|%>
	
	<% if invite.email_hash == params[:hash]%>
		<% current_user = {'role' => User::SPEAKER}%>
		<%= render 'form' %>
	<% else %>
		<p> yanki go	 home	! </p>
	<% end %>

<% end %>

<%= link_to 'Back', admin_speakers_path %>

</fieldset>