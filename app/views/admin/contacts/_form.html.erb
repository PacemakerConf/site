<%= form_for( [:admin, @contact],  html: {class: 'form-horizontal'} ) do |f| %>
 
  <div class="form-group">
    <%= f.label :name, "Name*", class: "col-lg-2 control-label"%>
    <div class="col-lg-7">
      <%= f.text_field :name, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
      <%= f.label :surname,"Surname*", class: "col-lg-2 control-label"%>
      <div class="col-lg-7">
        <%= f.text_field :surname, class: "form-control" %>
      </div>
  </div>

  <div class="form-group">
    <%= f.label :telephone, "Telephone*", class: "col-lg-2 control-label"%>
    <div class="col-lg-7">
      <%= f.text_field :telephone, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
      <%= f.label :email, "Email*", class: "col-lg-2 control-label"%>
      <a class="btn-default btn-sm btn-warning" id ="add_button">+</a>
      <div class="col-lg-7">
        <input class="form-control mail-adreess">
      </div>
  </div>
  
  <div id="mail-area"></div>

  <%= hidden_field( :contact, :email, class: 'allmails') %>
  
      <div class="form-group">
    <%= f.label :skype, class: "col-lg-2 control-label"%>
    <div class="col-lg-7">
      <%= f.text_field :skype, class: "form-control" %>
    </div>
    </div>
  <div class="form-group">
    <div class="col-lg-10 col-lg-offset-2">
      <%= f.submit class: "btn btn-primary"%>
    </div>
  </div>
<% end %>

<script type = "text/javascript">

  function RemoveMail(button){
    $(button).parent().remove();
  }

  var savedMails = $(".allmails").val();
  var lastSymbol = savedMails.charAt(savedMails.length-1);
  if(lastSymbol == ";"){
    savedMails = savedMails.substring(0, savedMails.length-1);
  }
  var mailsArray = savedMails.split(";");
  
  if(mailsArray){
  $.each(mailsArray,function(index, obj){

      $("#mail-area").append(INPUT_DIV);
      $(".mail-adreess:eq("+index+")").val(mailsArray[index]);
    });
    $(".mail-adreess:last").parents(':eq(1)').remove();


}
</script>



