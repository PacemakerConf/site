<%= form_for [:admin, @event],  html: {class: 'form-horizontal'}  do |f| %>
  <% if @event.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved:</h2>

      <ul>
      <% @event.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


  <div class="form-group">
    <% if params['conf_id'] %>
      <div class="form-group">
        <%= f.label :conference_id, 'Conference', class: "col-lg-2 control-label" %>
        <div class="col-lg-10">
          <input class="form-control"  disabled='true' type='text' placeholder='<%= params['conf_fname'] %>'>
        </div>
      </div>
      <%= hidden_field( :event, :conference_id, value: params['conf_id']) %>
    <% else %>
      <%= f.label :conference_id, 'Conference', class: "col-lg-2 control-label" %>
      <div class="col-lg-10">
        <%= select_tag 'event[conference_id]', options_from_collection_for_select(Conference.all, 'id', 'fullname', @event.conference_id || Conference.last.id), class: "form-control"%>
      </div>
    <% end %>
  </div>

  <div class="form-group">
    <%= f.label :event_type_id, 'Event type*', class: "col-lg-2 control-label"%>
    <div class="col-lg-10">
      <%= select_tag 'event[event_type_id]', options_from_collection_for_select(EventType.all, 'id', 'name', @event.event_type_id || 1), class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :duration, class: "col-lg-2 control-label" %>
    <div id='event_duration_hour' class='col-lg-10'>
      <input type='text' id='event_hours' class='form-control time-field' onchange = 'setEventHoursDuration()' value='<%= @event.duration.hour || EventType.first.defaultDuration.hour %>'>
      :
      <input type='text' id='event_mins' class='form-control time-field' onchange = 'setEventMinsDuration()' value='<%= @event.duration.min || EventType.first.defaultDuration.min %>'>
    </div>
    <%= f.hidden_field :duration, value: @event.duration || EventType.first.defaultDuration %>
  </div>

  <div class="form-group">
    <%= f.label :title, 'Name*', class: "col-lg-2 control-label"%>
    <div id='event_duration_hour' class='col-lg-10'>
      <%= f.text_field :title, class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :description, class: "col-lg-2 control-label"%>
    <div class="col-lg-10">
      <%= f.text_area :description, class: "form-control", rows: "3" %>
    </div>
  </div>

  <div class="form-group">
    <%= f.label :speaker_id, 'Speaker', class: "col-lg-2 control-label"%>
    <div class="col-lg-10">
      <%= select_tag 'event[speaker_id]', content_tag(:option,'No speaker', value: "")+options_from_collection_for_select(Speaker.all, 'id', 'name', @event.speaker_id || ''), class: "form-control" %>
    </div>
  </div>

  <div class="form-group">
    <div class="col-lg-10 col-lg-offset-2">
      <%= f.submit %>
    </div>
  </div>
<% end %>