{
	"fullDuration" : "<%= @event_type.defaultDuration.to_s %>",
	"durationHour" : "<%= @event_type.defaultDuration.hour.to_s %>",
	"durationMin" : "<%= @event_type.defaultDuration.min.to_s %>",
	
	"speakerEvent" : "<%= @event_type.speakerEvent.to_i %>"
}